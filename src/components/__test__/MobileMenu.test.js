import React from 'react'
import '../../../enzyme.config'
import { shallow } from 'enzyme'

import MobileMenu from '../nav/MobileMenu'

describe('maybe test', () => {
  it('should probaly have some tests', () => {
    expect(true).toBeTruthy
  })
})
